# Formal review dispatcher dogfood target

This draft-only file exists solely to receive one harmless typed `COMMENT` pull-request review for #1502 dogfood.

The target pull request should be closed unmerged after provider readback is captured.
